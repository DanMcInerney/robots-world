# New-session execution prompt

> **Superseded for new work, 2026-09-21:** the "full relevant menus" instruction below (and this document's 43,218-tuple factored menu, used throughout the five rounds) is superseded by F57–F61/F75's small bounded per-option-consequence menus, which are what actually produced measured gains; the five rounds run under this prompt reached 0/24 full-mission passes (F43). See the [find-and-follow ladder](jev-find-follow-ladder.md) (F78) for the current test design. This document's mission sentence and campaign-hygiene requirements (freezes, immutable snapshots, fresh confirmation seeds, disjoint development/confirmation) remain sound and are reused there.

Work in the existing checkout `C:/Users/danhm/tools/robots-world`, even if this task starts in a different directory. Preserve its current branch, all uncommitted/untracked work and ignored experiment evidence. Read `AGENTS.md`, `README.md`, `PRINCIPLES.md`, `docs/design.md`, `docs/design-failures.md`, `docs/jev-spatial-awareness-research.md`, and especially `docs/JEV-FIVE-ROUND-HANDOFF.md` before changing anything. The handoff contains the prior test results, cited research, implementation entry points, hypotheses, experiment rules and resource ceilings.

Execute five adaptive rounds of real TypeSafe Jev experiments in Robots World: design tests that isolate individual hypotheses, implement and qualify them, freeze their configurations, run them with the saved API key, deeply analyze results/logs/replays, update the accumulating design-failures document, then design the next round from that evidence. Continue through all five actual rounds; do not stop after a plan or the first batch. Maintain durable campaign state so interruptions do not lose progress or repeat charged tests. If a genuine blocker prevents completion, retain everything and state exactly which rounds remain incomplete.

The English mission, repeated in every main-task request, is: "Find the blue car in this unfamiliar area and follow it as it moves. It may not be visible initially. Choose where to look and move, keep it in view, avoid colliding with surfaces, and find it again after losing sight." Append explicit operating limits and control semantics consistently across paired arms. Component diagnostics are allowed but are not complete-mission success.

Jev must choose search, target, navigation, speed and camera commands. The primary candidate must not secretly use an automatic scan, camera-centering servo, target-follow script, route planner, recommendation-bearing state or privileged simulator target geometry. Ordinary perception, sensor-derived spatial memory, calibrated coordinate conversion, actuator mapping and low-level stabilization are allowed and must be attributable. Only installed sensors and libraries processing their actual outputs may supply Jev's facts. Keep unknown geometry unknown. Prior assisted successes are references, not the requested solution.

Start with small comparisons addressing missed detection events, explicit action semantics, compact spatial/view memory and complete factored controls. The handoff proposes 88 flights across five rounds, adjustable from evidence, with final fresh-seed confirmation and an information-removal ablation. Do not change multiple factors within a claimed single-factor comparison. Distinguish development, within-round evaluation and untouched final confirmation; retain failed and infrastructure-invalid attempts. Keep physics/sensing running during inference and normally run one simulated world at a time.

Archive immutable executable source/assets/configuration/lockfile snapshots for every round, not just hashes. Final confirmation uses eight frozen case/seed/fixture blocks: two turn-to-find, three obstacle-translation and three loss/recovery, including at least two newly qualified structural layouts. Unused seeds that merely rotate/mirror the old course do not establish unfamiliar-topology performance; state the limitation if that coverage cannot be qualified.

Jev allows 255 options per Choice, not per complete decision. Batched questions are independent; their IDs are not model-visible. Use full relevant menus and explicit conditional branches where needed. Do not confuse state compression with enumerating all joint actuator combinations. Recheck official documentation and pin/log the actual model. No unqualified confidence threshold or numeric Score-to-motor interpolation.

Real Jev inference in simulation is authorized using `.env.jev.local`; never expose credentials. Observe the handoff's campaign ceilings: 160 real flights, 40,000 API requests, 500 million input tokens including conservative uncertain-call reservations, approximately $21 at the documented rate. Use smaller allocations to reserve capacity for every round; do not silently raise limits or add other paid model services. No physical hardware, purchases or publishing. Respect service errors and record any explicit recovery before resuming.

Keep Robots World simple, modular and controller-neutral. Prefer experiment-layer changes. Nervelet at `C:/Users/danhm/tools/nervelet` may be changed only for a demonstrated reusable need, after reading its own instructions/contracts; preserve native-harness ownership and independent acquisition. Do not claim these direct Jev experiments qualify a native agent or real autopilot.

After every round append concise evidence and design rationale to `docs/design-failures.md`, update `docs/jev-spatial-five-round-results.md`, and deliver a local visual report containing every trial, exact mission/state/questions/options, raw sensor images, perception outputs, probabilities, commands/MAVLink and timing, costs, successes and representative failures. Maintain a five-round index under a new `.runtime/experiments/jev-spatial-five-rounds-v1/` root, or safely resume an existing recorded campaign. Run appropriate tests/typecheck/build and browser verification for changes. Finish by opening the complete visual report and explaining the simplest supported technique, the role Jev actually played, unresolved failures and hardware/compute limitations. No commit or PR is requested.
