import {readFile,writeFile} from 'node:fs/promises';
import {resolve} from 'node:path';
const root=resolve(process.argv[2]??'.runtime/experiments/jev-scout-techniques-v1');
const read=async p=>JSON.parse(await readFile(resolve(root,p),'utf8'));
const freeze=await read('freeze.json'),usage=await read('usage.json').catch(()=>null),runs=[],invalid=[],stages=[];
for(const kind of freeze.cases){const directory=`pilot-${kind}`,report=await read(`${directory}/report.json`).catch(()=>null);if(!report)continue;stages.push({kind,complete:report.complete,stopped:report.stopped});
 for(const row of report.runs){const run=await read(`${directory}/${row.file}`),single={...report,manifest:{...report.manifest,arms:[row.arm],seeds:[row.seed]},runs:[row],invalid:[]};
  const blueInputs=run.decisions.filter(d=>d.request.state.objects.some(o=>o.appearance.startsWith('blue'))),perception={firstBlueInputMs:blueInputs[0]?.observation.simMs??null,blueInputCount:blueInputs.length};
  await writeFile(resolve(root,directory,`${row.id}.view.json`),JSON.stringify(single));runs.push({kind,directory,...row,scout:run.scout,perception,audit:run.audit,maxLagMs:run.maxLagMs,stats:run.stats});}
 for(const row of report.invalid)invalid.push({kind,directory,...row});
}
const planned=freeze.cases.length*freeze.development.length*Object.keys(freeze.arms).length;
const aggregates=Object.entries(freeze.arms).map(([arm,definition])=>{const rows=runs.filter(r=>r.arm===arm),mean=fn=>rows.length?rows.reduce((s,r)=>s+fn(r),0)/rows.length:null,sum=fn=>rows.reduce((s,r)=>s+fn(r),0);return{arm,...definition,completed:rows.length,planned:planned/Object.keys(freeze.arms).length,passed:rows.filter(r=>r.scout.success).length,detected:rows.filter(r=>r.scout.firstDetectionMs!==null).length,jevDetected:rows.filter(r=>r.perception.firstBlueInputMs!==null).length,visibleSeconds:sum(r=>r.scout.visibleFraction*r.scout.postAcquisitionMs)/1000,centredSeconds:sum(r=>r.scout.centredFraction*r.scout.postAcquisitionMs)/1000,framedSeconds:sum(r=>r.scout.framingFraction*r.scout.postAcquisitionMs)/1000,meanFraming:mean(r=>r.scout.framingFraction),meanPathM:mean(r=>r.metrics.pathM),contactFlights:rows.filter(r=>r.metrics.collisionTicks).length,boundsFlights:rows.filter(r=>r.metrics.boundsTicks).length};});
const analysis={planned,completed:runs.length,invalid,unstarted:planned-runs.length-invalid.length,complete:stages.length===freeze.cases.length&&stages.every(s=>s.complete),stages,usage,aggregates,runs};
await writeFile(resolve(root,'analysis.json'),JSON.stringify(analysis,null,2));
const esc=v=>String(v??'—').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const num=(v,n=1)=>v==null?'—':(Math.round(Number(v)*10**n+1e-7)/10**n).toFixed(n),pct=v=>v==null?'—':`${num(v*100)}%`;
const replay=r=>`/pixels.html?report=${encodeURIComponent(`/.runtime/experiments/${root.split(/[\\/]/).at(-1)}/${r.directory}/${r.id}.view.json`)}`;
const best=[...runs].sort((a,b)=>b.scout.framingFraction*b.scout.postAcquisitionMs-a.scout.framingFraction*a.scout.postAcquisitionMs)[0];
function timeline(r){return `<svg viewBox="0 0 600 12" role="img" aria-label="Visible blue, fully framed green, otherwise grey over 120 seconds"><rect width="600" height="12" fill="#303d4e"/>${r.scout.timeline.filter(f=>f.visible).map(f=>`<rect x="${f.simMs/200}" y="0" width="1.3" height="12" fill="${f.framed?'#66e7a5':'#65aaff'}"/>`).join('')}</svg>`;}
const html=`<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Jev · Three scouting techniques</title><style>
*{box-sizing:border-box}body{background:#0c1420;color:#e5edf8;font:15px/1.55 system-ui;margin:0}main{max-width:1400px;margin:auto;padding:32px}a{color:#89c1ff}h1{font-size:38px;line-height:1.1;margin:14px 0}h2{font-size:23px}p{max-width:1000px;color:#b9c9dd}.eyebrow{color:#72dfa7;text-transform:uppercase;letter-spacing:.12em;font-size:12px}.grid{display:grid;grid-template-columns:repeat(3,1fr);gap:16px}.card,section{background:#141f2e;border:1px solid #304056;border-radius:12px;padding:20px;margin:20px 0}.card{margin:0}.stat{font-size:26px;color:#fff}.pill{display:inline-block;padding:3px 9px;border-radius:5px;background:#23344b;color:#d6e7ff}table{width:100%;border-collapse:collapse;min-width:900px}th,td{text-align:left;border-bottom:1px solid #314052;padding:12px 8px;vertical-align:top}th{color:#a8bdd6;font-size:12px}td{font-variant-numeric:tabular-nums}small{color:#a8bdd6}svg{width:180px;height:12px;display:block;margin-top:9px}.scroll{overflow:auto}.good{color:#74eab0}.bad{color:#ffbe8b}pre{white-space:pre-wrap;overflow-wrap:anywhere;font-size:12px}button{background:#2b4b70;color:white;border:0;border-radius:5px;padding:8px 14px;cursor:pointer}.flow{display:flex;gap:12px;flex-wrap:wrap;align-items:center}.flow b{padding:14px;background:#23344b;border-radius:8px}@media(max-width:800px){.grid{grid-template-columns:1fr}main{padding:16px}h1{font-size:30px}}</style></head><body><main>
<a href="/">Robots World</a><p class="eyebrow">Real Jev inference · camera-only pilot · obstacles</p><h1>Can Jev find it, then keep following?</h1><p>Three techniques, the same moving blue object and the same sensors. Every flight starts without the target visible. Camera assistance and dated memory are disclosed separately.</p>
<span class="pill">${runs.length}/${planned} complete</span> <span class="pill">${invalid.length} invalid · ${analysis.unstarted} unstarted</span> <span class="pill">${analysis.complete?'MATRIX COMPLETE':'MATRIX INCOMPLETE'}</span> <button onclick="location.reload()">Refresh report</button>
<div class="grid">${aggregates.map(a=>`<div class="card"><h2>${esc(a.label)}</h2><p>${esc(a.description)}</p><div class="stat">${a.passed}/${a.completed} mission passes</div><small>Jev saw target ${a.jevDetected}/${a.completed} · camera saw ${a.detected}/${a.completed}<br><strong>${num(a.visibleSeconds)} s visible · ${num(a.centredSeconds)} s centered</strong><br>${num(a.framedSeconds)} s fully framed / ${a.completed*freeze.seconds} s flown<br>${a.contactFlights} contact flights · ${a.boundsFlights} bounds flights<br>${a.completed}/${a.planned} planned flights complete</small></div>`).join('')}</div>
${best&&best.scout.framingFraction>0?`<section><h2>Most full framing in one flight</h2><p><strong>${esc(freeze.arms[best.arm].label)} · ${esc(best.kind)} · layout ${best.seed}</strong>: ${num(best.scout.framingFraction*best.scout.postAcquisitionMs/1000)} fully framed seconds; longest continuous stretch ${num(best.scout.longestFramedMs/1000)} seconds. ${best.scout.success?'Mission passed.':'This is partial progress; the complete mission failed.'}</p><p><a href="${replay(best)}">Watch this flight and inspect its decisions →</a></p></section>`:''}
<section><h2>One change at a time</h2><div class="flow"><b>RGB pixels + measured camera attitude</b>→<b>OpenCV regions → compact text</b>→<b>Jev: search / hold / select region + controls</b>→<b>MAVLink + camera adapter</b></div><p>Technique 2 adds measured-bearing camera centering and wide FOV to technique 1. Technique 3 adds dated visual memory to technique 2. Jev chooses every movement and every search adjustment. No route, rangefinder, map, hidden position, recommended answer or automatic search is provided. The plant is stabilized; this is not raw motor control.</p><p>Each search call offers 7 forward × 7 lateral × 7 vertical × 9 yaw × 7 pitch × 2 zoom settings: <strong>43,218 combinations</strong>, represented by six independent questions. One mode question chooses the branch. Every observed region gets its own conditional controls: six in direct, three movement controls with assistance. Nothing depends on a sibling answer. Memory is historical evidence, never a current location claim.</p><details><summary>Exact English instruction</summary><p>${esc((await read('pilot-turn-to-find/report.json').catch(()=>null))?.manifest.goals?.[0]??'See frozen scenario.ts')}</p></details></section>
${freeze.cases.map(kind=>`<section><h2>${esc(kind.replaceAll('-',' '))}</h2><p>${kind==='turn-to-find'?'The object stays outside the initial view. Waiting cannot reveal it.':kind==='behind-wall'?'A tall opaque wall blocks the object. Turning in place cannot reveal it; the drone must change viewpoint.':'The object moves around a structure, disappearing and reappearing. Some reappearance occurs without search, so recovery counts do not prove causation.'}</p><div class="scroll"><table><thead><tr><th>Technique / seed</th><th>First seen: camera / Jev</th><th>Full framing*</th><th>Visible / centered</th><th>Longest frame</th><th>Recovered losses</th><th>Path</th><th>Contacts / bounds**</th><th>Applied search</th><th>Replay / 120 s visibility</th></tr></thead><tbody>${runs.filter(r=>r.kind===kind).sort((a,b)=>a.seed-b.seed||Object.keys(freeze.arms).indexOf(a.arm)-Object.keys(freeze.arms).indexOf(b.arm)).map(r=>`<tr><td>${esc(freeze.arms[r.arm].label)}<br><small>${r.seed} · <span class="${r.scout.success?'good':'bad'}">${r.scout.success?'PASS':'FAIL'}</span></small></td><td>${r.scout.firstDetectionMs===null?'Never':`${num(r.scout.firstDetectionMs/1000)} s`}<br><small>Jev ${r.perception.firstBlueInputMs===null?'never':`${num(r.perception.firstBlueInputMs/1000)} s`}</small></td><td>${pct(r.scout.framingFraction)}</td><td>${num(r.scout.visibleFraction*r.scout.postAcquisitionMs/1000)} s / ${num(r.scout.centredFraction*r.scout.postAcquisitionMs/1000)} s</td><td>${num(r.scout.longestFramedMs/1000)} s</td><td>${r.scout.recovered}/${r.scout.losses.length}</td><td>${num(r.metrics.pathM)} m</td><td>${r.metrics.collisionTicks} / ${r.metrics.boundsTicks}</td><td>${r.scout.searchApplied}<br><small>${r.scout.searchWithTranslation} moving</small></td><td><a href="${replay(r)}">Watch + inspect exact calls</a>${timeline(r)}</td></tr>`).join('')||'<tr><td colspan="10">No completed flights yet.</td></tr>'}</tbody></table></div><p><a href="/pixels.html?report=${encodeURIComponent(`/.runtime/experiments/${root.split(/[\\/]/).at(-1)}/pilot-${kind}/report.json`)}">Compare two techniques side by side</a></p></section>`).join('')}
<section><h2>How to interpret this</h2><p>Camera first seen is an acquired-image detection; Jev first seen is the first decision input containing blue. Short detections can fall between calls. Card totals include the whole flight duration, including discovery delay and never-seen flights. *Full framing is measured from first detection to the end: visible patch centered, 8–14% image width, unclipped, wide FOV. Missing frames/target earn no credit. Never acquired = 0%. Green timeline = fully framed, blue = visible, grey = absent or unmeasured. **Contact and bounds values count physics ticks, not separate impacts.</p><p>PASS requires detection by 45 s, ≥30 s left, ≥40% full framing, ≥2 s continuous framing, no loss &gt;20 s, contacts, bounds violations or controller errors. Partial progress remains visible. The inherited altitude/position envelope was not stated in the English goal, and altitude/position estimates were not supplied; boundary failures are not evidence of ignoring known limits. Two pilot seeds cannot establish a robust winner. Synthetic box optics and simplified dynamics do not qualify deployment on hardware.</p><p><a href="../jev-scout-qualification-v3/index.html">Fixed-observer scene qualification</a> · <a href="freeze.json">Frozen plan</a> · <a href="source/docs/jev-scout-techniques.md">Design and limits</a> · <a href="analysis.json">Full results and audits</a> · <a href="diagnostics.json">Decision diagnostics</a> · <a href="results.md">Findings and limitations</a> · <a href="usage.json">Usage ledger</a></p><details><summary>Invalid attempts and accounting</summary><pre>${esc(JSON.stringify({invalid,usage,stages},null,2))}</pre></details></section></main></body></html>`;
await writeFile(resolve(root,'index.html'),html);console.log(JSON.stringify({completed:runs.length,invalid:invalid.length,unstarted:analysis.unstarted,aggregates}));
